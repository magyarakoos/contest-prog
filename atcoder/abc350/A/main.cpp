#include <bits/stdc++.h>
using namespace std;
using ll = long long;

#define cases true
void solve() {
    int X;
    cin >> X;
    cout << X << "\n";
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T = 1;
    if (cases) cin >> T;
    while (T--) solve();
}