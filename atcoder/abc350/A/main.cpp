#include <bits/stdc++.h>
using namespace std;
using ll = long long;

constexpr bool cases = true;
void solve() {
    cout << "Hi mom\n";
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T = 1;
    if (cases) cin >> T;
    while (T--) solve();
}