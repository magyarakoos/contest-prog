#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int N, A, X, Y;
map<int, double> cache;

double recur()

#define cases 0
void solve() {
    cin >> N >> A >> X >> Y;
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T = 1;
    if (cases) cin >> T;
    while (T--) solve();
}