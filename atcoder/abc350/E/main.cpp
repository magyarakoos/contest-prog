#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int N, A, X, Y;
map<int, double> cache;

double dp(int u) {

}

#define cases 0
void solve() {
    cin >> N >> A >> X >> Y;
    cout << setprecision(15) << 
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T = 1;
    if (cases) cin >> T;
    while (T--) solve();
}