#include <bits/stdc++.h>
using namespace std;
int N;
vector<int> a;

bool solve(int l, int r, bool turn) {
    
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);

    cin >> N;
    a.resize(N);
    for (int i = 0; i < N; i++) cin >> a[i];
    
}