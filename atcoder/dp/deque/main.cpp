#include <bits/stdc++.h>
using namespace std;
using ll = int64_t;

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int N;
    cin >> N;
    vector v(N, 0);
    for (int& x : v) cin >> x;

    
}
