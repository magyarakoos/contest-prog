#include <bits/stdc++.h>
using namespace std;
using u8 = unsigned char;

double solve(vector<u8> v) {
    if ()
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int N;
    cin >> N;
    vector<u8> v(N);
    for (u8& x : v) cin >> x;
    cout << solve(v);
}
