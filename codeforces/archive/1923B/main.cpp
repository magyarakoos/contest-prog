#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void solve() {
    int N, K;
    cin >> N >> K;
    vector<int> v(N + 1);
    
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    while (T--) solve();
}