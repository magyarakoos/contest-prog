#include <bits/stdc++.h>
using namespace std;

void solve() {
    int K, Q;
    cin >> K >> Q;
    vector<bool> v(100);
    while (K--) {
        int A;
        cin >> A;
        v[A - 1] = 1;
    }
    while (Q--) {
        int N;
        cin >> N;
        set<
    }
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) solve();
}