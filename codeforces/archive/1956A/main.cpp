#include <bits/stdc++.h>
using namespace std;

void solve() {
    int K, Q;
    cin >> K >> Q;
    set<int> s;
    while (K--) {
        int A;
        cin >> A;
        s.insert(A);
    }
    while (Q--) {
        int N;
        cin >> N;
        for (int i = 1; i <= N; i++) {
            
        }
    }
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) solve();
}