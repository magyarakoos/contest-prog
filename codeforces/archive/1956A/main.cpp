#include <bits/stdc++.h>
using namespace std;
#define all(v) v.begin(), v.end()

void solve() {
    int K, Q;
    cin >> K >> Q;
    set<int> s;
    while (K--) {
        int A;
        cin >> A;
        s.insert(A);
    }
    while (Q--) {
        cout << min(N, )
    }
    cout << "\n";
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) solve();
}