#include <bits/stdc++.h>
using namespace std;

void solve() {
    int K, Q;
    cin >> K >> Q;
    bitset<100> A;
    A |= 0;
    while (Q--) {
        int N;
        cin >> N;
        auto a = A;
        vector<int> b =
    }
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) solve();
}