#include <iostream>

using namespace std;

void solve() {
    int N, M;
    cin >> N >> M;
    vector<string> a(N, string(M, '$'));
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) solve();
}
