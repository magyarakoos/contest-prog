#include <array>
#include <iostream>
#include <vector>

using namespace std;

void solve() {
    int N;
    cin >> N;
    vector<array<bool, 2>> a(N, {0, 0});
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) solve();
}
