#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin >> N;
    int mx = INT_MIN;
    map<string, int> scoreS, firstS;
    while (N--) {
        string name;
        int score;
        cin >> name >> score;
        scoreS[name] += score;
        if (!)
    }
}
