#include <bits/stdc++.h>
using namespace std;
using ll = long long;
constexpr int MOD = 1e9 + 7;

int choose[1001][1001];
int main() {
	cin.tie(0), ios::sync_with_stdio(0);

    int K;
    cin >> K;
    vector<int> v(K);
    for (int& c : v) cin >> c;
}
