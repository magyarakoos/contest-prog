#include <bits/stdc++.h>
using namespace std;

void solve() {
    vector<array<int, 2>> v;
    v.push_back({INT_MAX, INT_MAX});
    int N, res = 0;
    cin >> N;
    while (N--) {
        int X;
        cin >> X;
        if ()
    }
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) solve();
}