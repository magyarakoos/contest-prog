#include <bits/stdc++.h>
using namespace std;

void solve() {
    int N;
    cin >> N;
    vector<int> v(N);
    for (int& x : v) cin >> x;
    vector<array<int, 2>> v;
    v.push_back({INT_MAX, INT_MAX});
}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int T;
    cin >> T;
    while (T--) solve();
}