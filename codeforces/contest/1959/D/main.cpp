#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
        int N;
        char c;
        cin >> N >> c;
        string s;
        cin >> s;
        s += s;

        vector<int> fc(s.size());
    }
}