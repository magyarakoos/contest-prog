#include <iostream>
#include <vector>

using namespace std;

const int MAXN = 1e9;

int main() {
    int N, P;
    cin >> N >> P;

    vector<int> a;
    for (int x = 0; (1 << x) + P <= MAXN; x++) {}
}
