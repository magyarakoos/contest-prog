#include <bits/stdc++.h>
using namespace std;

const int MAXN = 5e5 + 1;
int N, a[MAXN], b[MAXN];

bool solve(int ans) {}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    cin >> N;
    for (int i = 0; i < N; i++) cin >> a[i] >> b[i];
    for (int i = 20; i >) }
