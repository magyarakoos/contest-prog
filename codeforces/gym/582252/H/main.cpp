#include <bits/stdc++.h>
using namespace std;

int N, a[1 << 19], b[1 << 19];

bool solve(int ans) {}

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    cin >> N;
    for (int i = 0; i < N; i++) cin >> a[i] >> b[i];
    for (int i = 20; i >= 0; i--) solve(i);
}
