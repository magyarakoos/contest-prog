#include <bits/stdc++.h>
using namespace std;
#define int int64_t
using point = array<int, 2>;

void solve() {
    set<point> s;
    int n;
    cin >> n;
    while (n--) {}
}

int32_t main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int t;
    cin >> t;
    while (t--) solve();
}
