#include <iostream>
#include <vector>

using namespace std;
using ll = long long;

int main() {
    ll N, X;
    cin >> N >> X;
    vector<int> a(N);
    for (int& x : a) cin >> x;

    int l = 0, r = 0;
    ll sum = 0;
    while (r < N) {
    }
}
