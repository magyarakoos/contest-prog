#include <bits/stdc++.h>
using namespace std;

bool dp[(int)1e5 + 1];
int main() {
    int N;
    cin >> N;
    vector<int> v(N);
    for (int& x : v) cin >> x;

    vector<int> result;
    for (int i = 1; i <= N; i++) {

    }
}
