#include <bits/stdc++.h>
using namespace std;

#define all(v) v.begin(), v.end()
#define sz(v) (int)v.size()

int main() {
    int N;
    cin >> N;
    vector<int> v(N);
    for (int& x : v) cin >> x;


}
