#include <bits/stdc++.h>
using namespace std;
constexpr int INF 

int main() {
    cin.tie(0), ios::sync_with_stdio(0);

    int N, T, H;
    cin >> N >> T >> H;
    vector<array<int, 2>> v(N + 1);
    for (int i = 1; i <= N; i++) {
        cin >> v[i][0] >> v[i][1];
    }

    vector<int> dp(N + 2,)

}