#include <bits/stdc++.h>
using namespace std;

vector<int> schoolS, capS;

int main() {
    int N, M;
    cin >> N >> M;
    schoolS.resize(M);
    capS.resize(M);
    for (int i = 0; i < M; i++) {
        cin >> capS[i];
    }
}