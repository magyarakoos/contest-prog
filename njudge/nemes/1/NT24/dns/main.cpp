#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
	cin.tie(0), ios::sync_with_stdio(0);
    
    int N, result = 0, a_count = 0;
    cin >> N;

    while (N--) {
        char C;
        cin >> C;

        
    }    
}
