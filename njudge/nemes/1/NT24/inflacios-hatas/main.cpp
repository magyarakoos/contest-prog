#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    cin.tie(0), ios::sync_with_stdio(0);

    int N, K;
    cin >> N >> K;
    vector<int> v(N);
    for (int& x : v) cin >> x;

    priority_queue<int, vector<int>, greater<int>> pq(v.begin(), v.end());

    ll result = 0;

    while ()
}