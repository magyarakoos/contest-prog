#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0), ios::sync_with_stdio(0);

    int N, K;
    cin >> N >> K;
    vector<int> v(N);
    for (int& x : v) cin >> x;

    priority_queue<
}