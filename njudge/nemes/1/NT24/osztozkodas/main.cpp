#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N, K;
    cin >> N >> K;
    vector<ll> v(N), ps(N + 1);
    for (ll& x : v) cin >> x;
    sort(v.begin(), v.end());

    for (int i = 1; i <= N; i++) {
        
    }

    bool a_turn = 1;


}
