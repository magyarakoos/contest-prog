#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N, K;
    cin >> N >> K;
    vector<int> v(N);
    for (int& x : v) cin >> x;
    priority_queue<int> pq(v.begin(), v.end());
    bool a_turn = 1;
    ll A = 0, B = 0;
    while (!pq.empty()) {

        if ()

        a_turn ^= 1;
    }
}
