#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
    int N, K;
    cin >> N >> K;
    vector<int> v(N);
    for (int& x : v) cin >> x;
    sort(v.begin(), v.end());
    vector<ll> ps();
    bool a_turn = 1;
    ll A = 0, B = 0;
    while (!pq.empty()) {

        if (pq.size() <= K) {

        }

        a_turn ^= 1;
    }
}
