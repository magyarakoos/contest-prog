#include <array>
#include <iostream>
#include <queue>
#include <vector>

using namespace std;
using ll = long long;

int main() {
    cin.tie(0), ios::sync_with_stdio(0);

    ll M, N, Mdb = 1, Ndb = 1;
    cin >> M >> N;

    priority_queue<array<ll, 2>, vector<array<ll, 2>>,
                   greater<array<ll, 2>>>
        pq;
}
