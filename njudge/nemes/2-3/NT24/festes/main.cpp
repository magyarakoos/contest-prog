#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vec = vector;

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int N, M;
    cin >> N >> M;
    vector<ll> rowS(N);
    vector<vector<vector<ll>>> colS(M, vector<vector<ll>>(N, vector<ll>(N)));

}