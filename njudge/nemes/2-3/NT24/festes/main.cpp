#include <bits/stdc++.h>
using namespace std;
using ll = long long;
#define vec vector;

int main() {
    cin.tie(0), ios::sync_with_stdio(0);
    int N, M;
    cin >> N >> M;
    vec<ll> rowS(N);
    vec<vec<vec<ll>>> colS(M, vec<vec<ll>>(N, vec<ll>(N)));

}