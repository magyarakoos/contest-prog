#include <bits/stdc++.h>
using namespace std;
#define int int64_t

int32_t main() {
    cin.tie(0)->sync_with_stdio(0);
    string nl;
    do {
        int w, h, s;
        cin >> w >> h >> s;
    } while (getline(cin, nl));
}
